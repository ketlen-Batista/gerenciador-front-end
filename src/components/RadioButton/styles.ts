import MUIRadio from '@mui/material/Radio';
import { styled as MUIStyled } from '@mui/material/styles';

export const RadioButton = MUIStyled(MUIRadio)({
  padding: '0',
  marginRight: '0.25rem',
});
